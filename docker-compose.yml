services:
  workbench:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/backend/data
      - ./backend/memories:/app/backend/memories
    restart: unless-stopped
